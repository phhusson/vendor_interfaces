package vendor.qti.hardware.data.latency@1.0;

enum Level : uint32_t {
	L1 = 1,
	L2 = 2,
	L3 = 3,
	L4 = 4,
};

enum Radio : uint32_t {
	WWAN,
	WLAN,
};

enum StatusCode : uint32_t {
	OK,
	INVALID_ARGUMENTS,
	UNKNOWN_ERROR,
};
